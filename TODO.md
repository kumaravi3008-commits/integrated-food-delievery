# TODO: Replace Homepage Hero with Premium Marketing Hero

## Steps
- [x] 1. Gather information and create plan
- [x] 2. Get user approval on plan
- [x] 3. Edit `homepage.jsx` — Change import from `"./Hero"` to `"../../../components/layout/Hero/Hero"`
- [x] 4. Edit `homepage.jsx` — Remove `useState` import, `searchMode` state, `restaurantFilter`, and update props
- [x] 5. Verify no broken references

